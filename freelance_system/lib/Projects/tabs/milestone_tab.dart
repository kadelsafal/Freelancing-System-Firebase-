import 'package:flutter/material.dart';

class MilestoneTab extends StatelessWidget {
  const MilestoneTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No milestones have been set."),
    );
  }
}
